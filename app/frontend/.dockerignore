# Ignore node_modules — it's rebuilt during `npm install`
node_modules

# Ignore build artifacts — we rebuild during Docker build
build/

# Git and local config files
.git/
.gitignore
Dockerfile
.dockerignore

# OS or editor files
*.log
*.DS_Store
.env
.env.*
.vscode/